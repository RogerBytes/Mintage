sudo dpkg --configure -a
puis
sudo apt install -f

sudo apt update
sudo apt upgrade -y
sudo apt --fix-broken install

refaire
sudo dpkg --configure -a

Pour vérifier que c'est ok

si aucun retour le pb est réglé !

ensuite redémarrer direct
reboot
